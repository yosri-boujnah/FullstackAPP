// Define and export the sequelize model that represents the table freelances.

module.exports = (sequelize, DataTypes) => {
    const Freelances = sequelize.define("freelances", {
      
    });
  
    return Freelances;
  };