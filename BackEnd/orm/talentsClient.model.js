// Define and export the sequelize model that represents the table talents_has_client.

module.exports = (sequelize, DataTypes) => {
    const Talents_has_client = sequelize.define("talents_has_client", {
      
    });
  
    return Talents_has_client;
  };